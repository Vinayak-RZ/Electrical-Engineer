# Claude Code

1. Point Claude Code MCP at stdio command `electrical-engineer mcp`.
2. Attach `skills/<pack>/SKILL.md` as project skills if you want pedagogy next to the runner.
3. `run_workflow` never blocks on a human gate. Use the localhost UI for confirm.
4. Claude Code is one host among several. The CLI works without it.
