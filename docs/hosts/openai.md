# OpenAI / Codex

1. Configure the Codex/OpenAI MCP client with stdio `electrical-engineer mcp`.
2. Reuse the same `skills/` markdown; do not vendor a second recipe catalog.
3. Local OpenAI-compatible daemons are `EE_LOCAL_LLM_URL` on the CLI path only. This host uses its own loop. No BYOK in this graph.
4. Completeness of v1 does **not** require this host.
