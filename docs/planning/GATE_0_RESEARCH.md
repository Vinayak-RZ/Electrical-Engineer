# Gate 0 — Research, then questions

**Status:** Closed 2026-09-10. Answers compiled into [`IMPLEMENTATION_PLAN.md`](../../IMPLEMENTATION_PLAN.md) and [`EXECUTION_GRAPH.md`](../../EXECUTION_GRAPH.md). Node plans: [`plans/nodes/`](../../plans/nodes/). UI visual lock (later, also closed): [`DESIGN_LOCK.md`](DESIGN_LOCK.md).

Owner answers (summary): FastAPI + React slot UI (DSH-inspired, no Cordis); RAG-Anything capability first (LightRAG 1.5 spike) with research + decide after tests, QUALITY then SPEED; Spec Kit kept; all-pack solve+explain + compose-from-parts + C4/C5 in this graph; local LLM now, BYOK later; multimodal ingest only; improve PID/architecture first; commit budget 60–80; one branch; start after graph written; Composer 2.5 cheap / Grok 4.6 high; Ubuntu CI first; memory CLI; UI auto-open; workflow ids renamable; register EE nodes + cannot-do list.

The numbered question list that was asked lives in git history on this file’s first commit. Do not re-open Gate 0 unless the owner reopens P0 locks.
