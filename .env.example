# Local corpus (never commit real values with secrets)

# Directory of BYO PDFs you have rights to read (IITR library copies, etc.)
EE_CORPUS_DIR=.electrical-engineer/corpus

# Optional OpenAI-compatible daemon for solve-explain
# EE_LOCAL_LLM_URL=http://127.0.0.1:11434
# EE_LOCAL_LLM_MODEL=local
