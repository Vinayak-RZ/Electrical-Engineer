# Circuits pack — pedagogy only

Use Kirchhoff and Ohm before opening a simulator. Name the unknown, write KCL/KVL,
then check a number with `check-numeric` or SPICE. If the tool is missing, the CLI
prints the exact token `unchecked`. Do not invent a SPICE pass.
