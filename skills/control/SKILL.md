# Control pack — pedagogy only

Write the transfer function, list poles, then ask the library for step/Bode/root-locus.
MATLAB is optional. If `python-control` is missing, the run is `unchecked` — never invent a Bode bitmap.
