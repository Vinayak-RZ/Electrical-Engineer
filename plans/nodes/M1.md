# Node plan — M1 — Integrate

> Parent: [EXECUTION_GRAPH.md](../../EXECUTION_GRAPH.md)

| Field | Value |
|-------|-------|
| **Node id** | `M1` |
| **Job** | Barrier: wire CLI, UI, MCP, RAG, recipes, local LLM; tolerate cannot-do packs |
| **Wave** | 9 |
| **Depends on** | B_UI, B_RAG_SPIKE, B_PHOTO, B_LOCAL_LLM, B_C4_SIM, B_C5, B_HOST artifacts |
| **Write paths** | `src/electrical_engineer/__main__.py` glue, `tests/integration/test_wiring.py`, `PROGRESS.md` |
| **Read paths** | all prior outputs |
| **subagent_type** | lead |
| **Model** | `cursor-grok-4.6-high` |
| **Isolation** | lead only on shared glue |

---

## Objective

One process: CLI starts runner, `ui` serves the SPA, MCP calls the same run entry, retrieve-passage hits the RAG facade, classifier uses local LLM or skips. Missing RAG engine or local LLM does not block circuits. Missing pack depth is cannot-do, not a crash.

## Non-goals

- New features
- Replacing a slice that failed — record and continue if non-core

## Contract

**Input:** `{ "ui_server": true, "rag_facade": true, "photo_stub": true, "local_llm": true, "confirmed_sim": true, "c5_stub": true, "host_docs": true }`

**Output:** `{ "wired": true, "missing": [] }`

## Commits

| # | Commit | Gate |
|---|--------|------|
| 63 | `feat: wire CLI UI MCP RAG` | import cycle clean |
| 64 | `test: wiring integration` | pytest |

## Do not

- Start E1 if B_CORE or B_UI is missing
- Hide missing RAG behind a fake citation

## Return to graph

`wired`, `missing`, failures.
