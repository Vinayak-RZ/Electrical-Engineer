# Node plan — H1 — Harden

> Parent: [EXECUTION_GRAPH.md](../../EXECUTION_GRAPH.md)

| Field | Value |
|-------|-------|
| **Node id** | `H1` |
| **Job** | Full-repo validation, reviews, orchestrator green |
| **Wave** | 14 |
| **Depends on** | D1 `docs` |
| **Write paths** | `scripts/validate.sh`, extra tests, `PROGRESS.md`, `PHASE_N_COMPLETION.md` if used |
| **Read paths** | whole repo |
| **subagent_type** | lead (+ readonly reviews) |
| **Model** | `cursor-grok-4.6-high` |
| **Isolation** | lead commits fixes |

---

## Objective

Static: no Cordis/DSH dep, no `0.0.0.0`, no secrets, no commercial PDFs. Fast → medium → eval. `ponytail-review` on full diff; `ponytail-audit` if useful. `speckit-converge` if `.specify/` exists. Expand tests for T1 gaps. `./scripts/validate.sh` exits 0. Ubuntu CI green.

## Non-goals

- New product features
- Win/macOS CI

## Contract

**Input:** `{ "docs": true }`

**Output:** `{ "validate": true, "reviews": [], "done": true }`

## Commits

| # | Commit | Gate |
|---|--------|------|
| 70 | `chore: validate.sh + hardening tests` | `./scripts/validate.sh` |

## Do not

- Skip hardening because feature commits passed
- Merge with failing eval injection

## Return to graph

validate exit code, review findings, failures.
