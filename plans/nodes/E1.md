# Node plan — E1 — Evaluate

> Parent: [EXECUTION_GRAPH.md](../../EXECUTION_GRAPH.md)

| Field | Value |
|-------|-------|
| **Node id** | `E1` |
| **Job** | Gold items + `electrical-engineer eval` / `--pack`; injection cannot flip policy |
| **Wave** | 10 |
| **Depends on** | M1 `wired` |
| **Write paths** | `eval/gold/**`, `src/electrical_engineer/eval_runner/**`, `tests/integration/test_eval.py` |
| **Read paths** | `eval/gold/README.md` |
| **subagent_type** | generalPurpose |
| **Model** | `cursor-grok-4.6-high` |
| **Isolation** | eval paths |

---

## Objective

Licence-clean gold under circuits, control, unmatched, injection, plus at least a stub per other pack or a cannot-do pointer. Each item: `task.md`, `expect.json` (`recipe_id`, numeric tolerance or required `unchecked`). Runner compares `summary.json`. `EE_ALLOW_ALL=1` for CI gates **does not** disable unchecked scoring. No hosted leaderboard. No LLM-as-judge platform. No third-party exam PDFs.

## Non-goals

- 200-task bank
- Public leaderboard

## Contract

**Input:** `{ "wired": true }`

**Output:** `{ "eval_green": true, "packs": [], "injection_holds": true }`

## Commits

| # | Commit | Gate |
|---|--------|------|
| 61 | `test(eval): circuits + unmatched gold` | items exist |
| 62 | `test(eval): injection pack` | policy holds |
| 65 | `feat(eval): electrical-engineer eval` | `--pack circuits` |
| 66 | `test(eval): runner scoring` | pytest |

## Do not

- Commit copyrighted GATE papers
- Use LLM-as-judge as the only score

## Return to graph

`eval_green`, `injection_holds`, failures.
